#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../@gatsbyjs/relay-compiler/bin/relay-compiler" "$@"
  ret=$?
else 
  node  "$basedir/../@gatsbyjs/relay-compiler/bin/relay-compiler" "$@"
  ret=$?
fi
exit $ret
